import React from "react";

export default function SearchBar(props){
    return (
        <input id="searchbar" type="text"name="search" placeholder="Search a movie, an actor, a user..."></input>
    )
}